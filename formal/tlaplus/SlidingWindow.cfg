\* TLC Configuration for SlidingWindow.tla
\* NOMAD Anti-Replay Sliding Window Verification

\* Disable deadlock checking - model explores all reachable states
\* Quiescence when MaxFrames reached is expected
CHECK_DEADLOCK FALSE

CONSTANTS
    \* MODEL VALUES (small for tractable verification)
    \* These are isomorphic to spec values for safety property verification.
    \*
    \* Spec values (1-SECURITY.md §Anti-Replay Protection):
    \*   WindowSize = 2048 bits (2048 nonce slots)
    \*   MaxNonce   = 2^64-1
    \*
    \* Model uses small values to keep state space tractable.
    \* Key properties (ordering, DoS resistance) are scale-independent.
    \*
    \* Reduced bounds for tractable verification
    \* Key ordering properties are scale-independent
    WindowSize = 3
    MaxNonce = 5
    MaxFrames = 6

SPECIFICATION Spec

INVARIANTS
    TypeOK
    Safety
    OnlyAuthenticatedMarkedSeen
    WindowBounded
    FloorEnforced

\* Liveness not required - this is a safety/security model
\* The key property is that attackers cannot advance the window
