\* TLC Configuration for SyncLayer.tla
\* NOMAD Sync Layer Verification

\* Disable deadlock checking - quiescence (full sync) is expected
\* When both nodes have synced all state, no further actions are enabled
CHECK_DEADLOCK FALSE

CONSTANTS
    \* Minimal for quick verification - increase for thorough check
    MaxStateNum = 2
    MaxDiffValue = 1
    NumNodes = 2

SPECIFICATION Spec

INVARIANTS
    TypeOK
    Safety
    MonotonicStateNums
    AckedNeverExceedsSent
    PeerNeverAhead
    ValidMessages

\* Liveness DISABLED: UDP model allows infinite message loss, breaking any
\* liveness guarantee. Real convergence requires application-level retransmission
\* which is outside the sync layer scope. Safety is verified; liveness is an
\* application-layer concern (periodic sync messages ensure eventual delivery).
\*
\* PROPERTIES
\*     EventualConsistency
