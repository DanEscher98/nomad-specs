# Nomad Protocol Docker Configuration
#
# Copy this file to .env and customize as needed.
# These settings are used by docker-compose.yml

# =============================================================================
# Implementation Paths
# =============================================================================

# Path to your server implementation (default: use stub)
# SERVER_CONTEXT=/path/to/your/implementation
# SERVER_DOCKERFILE=Dockerfile

# Path to your client implementation (default: use stub)
# CLIENT_CONTEXT=/path/to/your/implementation
# CLIENT_DOCKERFILE=Dockerfile

# Build targets (server/client)
# SERVER_TARGET=server
# CLIENT_TARGET=client

# =============================================================================
# Cryptographic Keys (for testing only!)
# =============================================================================

# These are test-only keys. DO NOT USE IN PRODUCTION.
# In production, generate fresh keys with proper entropy.

# Server static keypair (base64-encoded)
SERVER_PRIVATE_KEY=SGVsbG8gV29ybGQhIFRoaXMgaXMgYSB0ZXN0IGtleQ==
SERVER_PUBLIC_KEY=VGVzdCBwdWJsaWMga2V5IGZvciBSb2FtIHByb3RvY29s

# =============================================================================
# Protocol Settings
# =============================================================================

# State type to use for testing
STATE_TYPE=nomad.echo.v1

# Log level (debug, info, warn, error)
LOG_LEVEL=debug

# =============================================================================
# Interoperability Testing
# =============================================================================

# Paths to different implementations for interop tests
# RUST_IMPL_PATH=/path/to/rust/nomad
# GO_IMPL_PATH=/path/to/go/nomad
